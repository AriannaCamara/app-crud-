{
  "name": "app-crud",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "json-server server/db.json --host 0.0.0.0 --port $PORT --static dist"
  },
  "devDependencies": {
    "json-server": "^0.17.4",
    "vite": "^8.2.0"
  }
}
